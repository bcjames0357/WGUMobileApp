<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wgumobileapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".TermDetailsActivity" />
        <activity android:name=".CourseDetailsActivity" />
        <activity android:name=".AssessmentDetailsActivity" />
        <activity android:name=".AddTermActivity" />
        <activity android:name=".AddMentorActivity" />
        <activity android:name=".AddCourseActivity" />
        <activity android:name=".AddAssessmentActivity" />
        <activity android:name=".ViewTermsActivity" />
        <activity android:name=".ViewMentorsActivity" />
        <activity android:name=".ViewAssessmentsActivity" />
        <activity android:name=".ViewCoursesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".Receivers.AlarmReceiver" />







        
    </application>

</manifest>